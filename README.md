## Rite Aid Covid Vaccine Finder

### How to run

- Install ruby via [rbenv](https://github.com/rbenv/rbenv)
- Install chromedriver via [homebrew](https://brew.sh://brew.sh/)
  - `brew tap homebrew/cask && brew cask install chromedriver`

```
bundle install
ruby book.rb
```
